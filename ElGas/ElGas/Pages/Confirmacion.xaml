<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:tk="clr-namespace:TK.CustomMap;assembly=TK.CustomMap"
             xmlns:numeric="clr-namespace:Syncfusion.SfNumericUpDown.XForms;assembly=Syncfusion.SfNumericUpDown.XForms"
             x:Class="ElGas.Pages.Confirmacion">
    <ContentPage.Content>
        <StackLayout>
            <tk:TKCustomMap HeightRequest="300"                 
                            Pins="{Binding Locations}" 
                            MapRegion="{Binding CenterSearch}"

></tk:TKCustomMap>
            <Label Text="Ingrese el número de tanques requeridos"></Label>
            <Entry Placeholder="Cantidad" Text="{Binding Cilindros,Mode=TwoWay}" Keyboard="Numeric" ></Entry>
            <Label Text="Total del pedido"></Label>
            <Label FontSize="Large" FontAttributes="Bold" Text="{Binding Valor}"></Label>

            <Button Text="Confirmar" Command="{Binding OkCommand}"></Button>




        </StackLayout>
    </ContentPage.Content>
</ContentPage>